Make That DATABASE

Generic:
    1. Open and activate a venv
    2. Install stripe, flask, sqlalchemy, flask-sqlalchemy, psycopg2
    3. In psql create a database named rd.
    4. Set the Private and public keys
    5. Set the enviornent variable FLASK_APP to app.py
    6. Run!

Windows
in powershell:
    python -m venv ReeDistribute
    scripts/activate
    psql -U postgres
        DROP DATABASE rd;
        CREATE DATABASE rd;
        \q;
    python -m install stripe
    python -m install flask
    python -m install sqlalchemy
    python -m install flask-sqlalchemy
    python -m install psycopg2
    $env:PUBLISHABLE_KEY='pk_test_6pRNASCoBOKtIshFeQd4XMUh'
    $env:SECRET_KEY='sk_test_BQokikJOvBiI2HlWgH4olfQ2'
    $env:FLASK_APP='app.py'
    python -m flask run



    